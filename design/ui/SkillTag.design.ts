import { theme } from '@ai/theme';

export const SkillTagDesign = {
  layout: {
    padding: '0.5rem 1rem',
    borderRadius: '9999px',
  },
  styles: {
    backgroundColor: '#293038', // Needs to be added to theme
    color: theme.themes.dark.colors.text,
    fontWeight: theme.typography.weights[1], // 500
    fontSize: '0.875rem',
  },
}; 